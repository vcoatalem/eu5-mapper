# EU5 MapApp

## Structure

* game_data folder -> raw extracts from game files
* tools -> python processing pipeline
* public -> where the python-processed game files are stored for frontend to load
* app -> the app
* workers -> sources of workers used by the app
* public/workers -> where the transpiled worker js files are store for frontend to load